# forge-image

> Images → tiles → Plato agents.

Decomposes images into regions with coordinate metadata. Grid, strip, or column decomposition. Each tile gets brightness, variance, and edge density metrics.

Plato agents process regions independently. The fleet sees the whole picture.

Apache 2.0
